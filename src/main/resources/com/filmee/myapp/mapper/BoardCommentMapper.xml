<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.filmee.myapp.mapper.BoardCommentMapper">

    <insert id="insert">
        <selectKey keyProperty="bcno" order="BEFORE" resultType="int">
            SELECT ISEQ$$_101443.nextval FROM dual
        </selectKey>
        <![CDATA[ 
            INSERT INTO fm_board_comment(bno,writer,content, parent_bcno)
            VALUES (#{bno}, #{writer}, #{content}, #{pbcno, jdbcType=VARCHAR})
        ]]>
    </insert>

    <select id="read" resultType="com.filmee.myapp.domain.BoardCommentVO">
        <![CDATA[ 
            SELECT /*+index_desc(fm_board)*/*
            FROM fm_board_comment
            WHERE bno=#{bno}
        ]]>
    </select>

    <delete id="delete">
        <![CDATA[
            DELETE FROM fm_board_comment
            WHERE bcno=#{bcno}
        ]]> 
    </delete>

    <update id="update">
        <![CDATA[ 
            UPDATE fm_board_comment 
            SET content=#{content},
            update_ts=sysdate
            WHERE bcno=#{bcno}
        ]]>
    </update>

</mapper>